import React from 'react';
import {
    CartModal,
    Header,
    Footer,
    ProductsList
} from '../../components';

export default () => (
    <section>
        <Header />
        <CartModal />
        <ProductsList />
        <Footer />
    </section>
);
